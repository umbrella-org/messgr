# Build artifacts
/messgr
/dist
/target

# scripts/e2e.sh runtime state (certs, pids, logs)
/.e2e

# Rust
*.test
*.out
.env

# Editor / OS
.DS_Store

# AI
.claude/settings.json
.claude/settings.local.json

# graphify - regenerated locally via `graphify` / the post-commit & post-checkout
# hooks (graphify hook install); not tracked, everyone rebuilds their own.
graphify-out/
